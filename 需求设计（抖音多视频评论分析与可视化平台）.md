# 需求设计（抖音多视频评论分析与可视化平台）

---

# 1. 项目背景

短视频行业持续高速发展，抖音作为头部平台积累了海量用户评论数据。评论内容能够直观反映观众对视频的观点、情绪倾向及地域分布。然而，人工复制整理评论数据操作繁琐、统计效率低下，无法快速完成情感分析与热度趋势跟踪。

为解决人工统计耗时费力、数据杂乱、分析不直观等痛点，本项目设计并开发"抖音多视频评论分析与可视化平台"。系统结合爬虫采集、数据清洗、后端服务与前端可视化技术，实现评论数据全链路自动化处理，为短视频创作者和数据分析人员提供一站式分析工具。

---

# 2. 项目目标

| 序号 | 目标 | 说明 |
|------|------|------|
| 1 | 自动化采集 | 用户输入视频编号，一键采集该视频下全部公开评论 |
| 2 | 多视频隔离 | 以视频编号为唯一标识，区分不同视频评论数据 |
| 3 | 规范化清洗 | 原始CSV清洗去重后批量入库，入库成功自动删除本地原始文件 |
| 4 | 多维分析 | 地区分布、情感倾向、点赞排行、时间趋势等统计维度 |
| 5 | 用户权限体系 | 手机号唯一注册、密码登录，区分游客/普通用户/管理员 |
| 6 | 可视化大屏 | 地图、饼图、折线图、排行榜等多图表直观展示 |
| 7 | 缓存加速 | Redis缓存热点数据，减轻数据库查询压力 |
| 8 | 前后端分离 | Vue3 + SpringBoot 架构，代码层次清晰，易于维护 |

---

# 3. 原始数据结构

> 参考数据文件：`Py_Data/Datas/7613237683142223214.csv`

| 原始字段 | 字段含义 | 示例值 |
|----------|----------|--------|
| 昵称 | 评论用户抖音昵称 | 用户0127、大转想早睡 |
| 地区 | 用户归属地（省份/直辖市/海外） | 山西、山东、湖北、美国 |
| 日期 | 评论发布时间 | 2026-03-04 12:35:19 |
| 内容 | 评论正文（含Emoji表情） | 遥想公瑾当年[泣不成声] |
| 点赞 | 评论点赞数量 | 152145 |

**数据特征分析**：
- 地区覆盖全国31个省级行政区及港澳台、海外
- 点赞数跨度极大（0 ~ 15万+），呈长尾分布
- 评论内容含大量Emoji表情与抖音平台特有符号（如`[捂脸]`、`[泣不成声]`）
- 时间跨度：单视频评论区通常覆盖数天至数月

---

# 4. 用户角色设计

## 4.1 游客

- 无需注册登录
- 仅可访问首页公开统计数据概览
- **不可**使用采集、清洗、详细评论查看、后台管理功能
- **不可**查看完整可视化图表

## 4.2 普通用户

- 手机号注册登录，手机号全系统唯一
- 权限范围：数据采集、数据清洗、多维分析、可视化大屏、评论查询、视频切换、个人中心
- **无**用户管理权限

## 4.3 管理员

- 拥有普通用户全部权限
- 额外：用户管理（查询全部用户、删除违规用户）
- 系统不设超级管理员，管理员之间权限平级

---

# 5. 核心功能模块

## 5.1 用户认证模块

| 子功能 | 描述 |
|--------|------|
| 手机号注册 | 校验手机号格式（11位）与唯一性，密码长度6-20位 |
| 账号登录 | 手机号+密码验证，密码BCrypt加密存储 |
| 登录态维持 | JWT Token + Redis缓存，Token有效期24小时 |
| 角色鉴权 | 后端拦截器根据角色区分接口访问权限 |
| 密码修改 | 需验证旧密码后更新 |
| 个人信息查看 | 展示注册手机号（脱敏）、注册时间、角色 |

**注册/登录规则**：
```
注册 → 校验手机号未被占用 → BCrypt加密密码 → 入库 → 返回成功
登录 → 查询手机号 → 校验密码 → 生成JWT → 写入Redis → 返回Token
```

## 5.2 评论数据采集模块

| 子功能 | 描述 |
|--------|------|
| 输入视频编号 | 前端表单输入抖音视频ID（纯数字） |
| 启动采集 | 后端接收请求，调用Python爬虫脚本 |
| 采集执行 | Selenium/requests模拟浏览器访问抖音评论接口 |
| 结果保存 | 以视频编号命名CSV文件，保存至 `Py_Data/Datas/` 目录 |
| 采集进度 | 后端返回采集状态（采集中/已完成/失败），前端轮询展示 |
| 错误处理 | 无效视频编号、接口限流、网络异常等场景提示用户 |

**采集字段映射**（抖音接口 → CSV文件）：
```
用户昵称 → 昵称
用户省份 → 地区
评论时间 → 日期
评论文本 → 内容
点赞数量 → 点赞
```

## 5.3 数据清洗处理模块

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 文件扫描 | 扫描 `Py_Data/Datas/` 目录下待处理CSV文件 |
| 2 | 编号提取 | 从文件名截取视频编号（如 `7613237683142223214.csv` → `7613237683142223214`） |
| 3 | 空值剔除 | 删除昵称或内容为空的行 |
| 4 | 去重 | 按（昵称 + 内容 + 日期）组合去重，保留最新记录 |
| 5 | 格式标准化 | 日期统一为 `yyyy-MM-dd HH:mm:ss`，Emoji保留原样 |
| 6 | 视频绑定 | 将视频编号写入每条记录 |
| 7 | 批量入库 | JDBC Batch Insert，每批500条 |
| 8 | 文件清理 | 入库成功后 `Files.delete()` 删除本地CSV |

**关键规则**：视频编号不从前端传入，由后端自动截取文件名生成，防止数据归属错误。

## 5.4 多维数据分析模块

### 5.4.1 基础统计

| 统计项 | 计算方式 |
|--------|----------|
| 评论总数 | `SELECT COUNT(*) FROM comment WHERE video_id = ?` |
| 参与用户数 | `SELECT COUNT(DISTINCT nickname) FROM comment WHERE video_id = ?` |
| 总点赞数 | `SELECT SUM(likes) FROM comment WHERE video_id = ?` |
| 平均点赞数 | 总点赞数 / 评论总数 |

### 5.4.2 地区分布统计

```
SELECT region, COUNT(*) AS cnt
FROM comment
WHERE video_id = ?
GROUP BY region
ORDER BY cnt DESC
```
- 输出：各省评论数及占比
- 包含港澳台及海外地区（如"美国"）分类

### 5.4.3 情感倾向分析

使用**情感词典 + 规则匹配**方式（适合毕设场景，无需引入大型NLP模型）：

| 情感类别 | 判断规则 |
|----------|----------|
| 积极 | 正面词得分 > 负面词得分 |
| 中性 | 正面词得分 = 负面词得分 或 无情感词 |
| 消极 | 负面词得分 > 正面词得分 |

- 内置中文情感词典（正面词约5000个，负面词约5000个）
- 支持Emoji情感映射（如`[泣不成声]` → 负面）
- 输出：三类情感评论数占比

### 5.4.4 时间趋势统计

```
SELECT DATE(publish_time) AS day, COUNT(*) AS cnt
FROM comment
WHERE video_id = ?
GROUP BY DATE(publish_time)
ORDER BY day
```
- 按天聚合评论数，生成时间序列
- 支持按小时聚合（粒度可选）

### 5.4.5 高赞评论排行

```
SELECT nickname, content, likes, region, publish_time
FROM comment
WHERE video_id = ?
ORDER BY likes DESC
LIMIT 20
```
- Top 20高赞评论列表

### 5.4.6 分析结果落库

每次分析完成后，将统计结果序列化为JSON存入 `analysis_result` 表，同时写入Redis缓存（TTL: 30分钟）。

## 5.5 可视化展示模块

| 图表组件 | 展示内容 | 图表类型 |
|----------|----------|----------|
| 数据概览卡片 | 评论总数、用户数、总点赞、平均点赞 | 数字统计卡 |
| 地区分布地图 | 全国各省评论数量分布 | ECharts中国地图 |
| 情感占比图 | 积极/中性/消极评论比例 | 环形饼图 |
| 时间趋势图 | 评论数随时间变化曲线 | 折线图（可缩放） |
| 高赞评论榜 | Top 20点赞最高评论 | 列表 + 滚动 |
| 省份TOP10柱状图 | 评论数最多的10个省份 | 横向柱状图 |

- 支持视频切换下拉框，切换后所有图表联动刷新
- 游客仅可查看概览卡片，其余图表需登录

## 5.6 评论列表模块

| 功能 | 描述 |
|------|------|
| 分页查询 | 每页20条，支持跳页 |
| 条件筛选 | 按地区、情感类别、点赞范围筛选 |
| 关键词搜索 | 评论内容模糊搜索 |
| 排序切换 | 按时间/点赞升序降序 |
| 导出 | 导出当前筛选结果为CSV（预留） |

## 5.7 后台用户管理模块（管理员专属）

| 功能 | 描述 |
|------|------|
| 用户列表 | 分页展示全部注册用户（手机号脱敏） |
| 查询 | 按手机号/角色/注册时间范围筛选 |
| 删除 | 删除违规用户及其关联操作记录 |
| 权限标识 | 列表明确标注用户角色 |

---

# 6. 页面清单与设计

| 序号 | 页面名称 | 路由 | 访问权限 | 核心功能 |
|------|----------|------|----------|----------|
| 1 | 登录页 | `/login` | 公开 | 手机号+密码登录表单 |
| 2 | 注册页 | `/register` | 公开 | 手机号+密码+确认密码注册表单 |
| 3 | 首页/数据大屏 | `/dashboard` | 游客可看概览，用户看完整 | 可视化图表集合，视频切换 |
| 4 | 数据采集页 | `/collect` | 登录用户 | 视频编号输入 + 采集状态展示 |
| 5 | 评论列表页 | `/comments` | 登录用户 | 分页表格 + 筛选 + 搜索 |
| 6 | 个人中心 | `/profile` | 登录用户 | 个人信息查看、修改密码、退出 |
| 7 | 用户管理页 | `/admin/users` | 管理员 | 用户列表 + 查询 + 删除 |

**页面布局规范**：
- 左侧导航栏（Logo + 菜单），右侧内容区
- 顶部栏（用户头像 + 角色标签 + 退出按钮）
- 响应式设计，适配1920×1080主流分辨率

---

# 7. 数据实体设计

## 7.1 用户表（`user`）

| 字段名 | 类型 | 长度 | 非空 | 说明 |
|--------|------|------|------|------|
| id | BIGINT | - | Y | 主键，自增 |
| username | VARCHAR | 50 | Y | 用户名（默认取手机号前3+后4位） |
| phone | VARCHAR | 11 | Y | 手机号，唯一索引 |
| password | VARCHAR | 128 | Y | BCrypt加密密文 |
| role | TINYINT | - | Y | 0=普通用户，1=管理员 |
| created_at | DATETIME | - | Y | 注册时间 |

- 唯一约束：`UNIQUE KEY uk_phone (phone)`
- 索引：`INDEX idx_role (role)`

## 7.2 评论表（`comment`）

| 字段名 | 类型 | 长度 | 非空 | 说明 |
|--------|------|------|------|------|
| id | BIGINT | - | Y | 主键，自增 |
| video_id | VARCHAR | 32 | Y | 视频编号（来源文件名） |
| nickname | VARCHAR | 100 | Y | 评论用户昵称 |
| region | VARCHAR | 50 | Y | 用户归属地区 |
| publish_time | DATETIME | - | Y | 评论发布时间 |
| content | TEXT | - | Y | 评论原文（含Emoji） |
| likes | INT | - | Y | 点赞数量，默认0 |
| sentiment | TINYINT | - | Y | 情感分类：1=积极，0=中性，-1=消极 |
| created_at | DATETIME | - | Y | 入库时间 |

- 联合索引：`INDEX idx_video_id (video_id)`
- 联合索引：`INDEX idx_video_region (video_id, region)`
- 联合索引：`INDEX idx_video_time (video_id, publish_time)`
- 联合索引：`INDEX idx_video_likes (video_id, likes DESC)`

## 7.3 分析结果表（`analysis_result`）

| 字段名 | 类型 | 长度 | 非空 | 说明 |
|--------|------|------|------|------|
| id | BIGINT | - | Y | 主键，自增 |
| video_id | VARCHAR | 32 | Y | 视频编号，唯一索引 |
| total_comments | INT | - | Y | 评论总数 |
| total_users | INT | - | Y | 参与评论用户数 |
| total_likes | BIGINT | - | Y | 总点赞数 |
| avg_likes | DECIMAL | (10,2) | Y | 平均点赞数 |
| region_stats | TEXT | - | Y | 地区统计JSON |
| sentiment_stats | TEXT | - | Y | 情感统计JSON |
| daily_stats | TEXT | - | Y | 每日评论数JSON |
| top_comments | TEXT | - | Y | 高赞评论JSON |
| analyzed_at | DATETIME | - | Y | 分析生成时间 |

- 唯一约束：`UNIQUE KEY uk_video_id (video_id)`

### 分析结果JSON格式示例

**region_stats**：
```json
[
  {"region": "山东", "count": 45, "percentage": 14.8},
  {"region": "广东", "count": 38, "percentage": 12.5}
]
```

**sentiment_stats**：
```json
[
  {"sentiment": "积极", "count": 120, "percentage": 39.7},
  {"sentiment": "中性", "count": 98, "percentage": 32.5},
  {"sentiment": "消极", "count": 84, "percentage": 27.8}
]
```

**daily_stats**：
```json
[
  {"date": "2026-03-04", "count": 87},
  {"date": "2026-03-05", "count": 42}
]
```

**top_comments**：
```json
[
  {"nickname": "用户0127", "content": "遥想公瑾当年", "likes": 152145, "region": "山西"}
]
```

---

# 8. 主要业务流程

## 8.1 用户注册登录流程

```
┌──────────┐     ┌──────────────┐     ┌──────────┐
│ 前端页面  │────▶│ SpringBoot   │────▶│  MySQL   │
│          │     │  Controller  │     │          │
└──────────┘     └──────────────┘     └──────────┘

注册流程：
1. 输入手机号 + 密码 + 确认密码
2. 前端校验：手机号格式、密码长度、两次密码一致
3. 后端校验：手机号是否已注册
4. BCrypt加密密码 → INSERT INTO user
5. 返回注册成功，跳转登录页

登录流程：
1. 输入手机号 + 密码
2. 后端查询 user 表 → BCrypt校验密码
3. 生成JWT（含userId、role）→ 写入Redis（key=token, TTL=24h）
4. 返回Token + 用户基本信息
5. 前端存储Token → 路由跳转至Dashboard
```

## 8.2 评论采集流程

```
用户输入视频编号 → 点击采集按钮
        │
        ▼
后端接收请求 → 校验视频编号格式
        │
        ▼
调用 Python 采集脚本（ProcessBuilder / Runtime.exec）
        │
        ▼
Python 脚本执行：
  ├── 模拟浏览器访问抖音视频页
  ├── 逐页抓取评论数据（昵称、地区、日期、内容、点赞）
  ├── 写入 CSV 文件 → Py_Data/Datas/{video_id}.csv
  └── 返回采集状态码
        │
        ▼
后端轮询脚本状态 → 返回前端（采集中/已完成/失败）
```

## 8.3 数据清洗入库流程

```
前端触发清洗请求（传入 video_id 或清洗全部待处理文件）
        │
        ▼
后端扫描 Py_Data/Datas/ 目录
        │
        ▼
遍历每个 .csv 文件：
  ├── 解析文件名 → 提取 video_id
  ├── 读取 CSV → 逐行解析
  ├── 空值检测 → 剔除 nickname/content 为空的行
  ├── 去重检查 → (nickname, content, publish_time) 组合
  ├── 情感分析 → 计算 sentiment 字段
  └── 批量组装 → JDBC Batch INSERT
        │
        ▼
入库成功 → Files.delete() 删除本地文件
        │
        ▼
返回清洗结果（总数、有效数、去重数、失败数）
```

## 8.4 数据分析与缓存流程

```
前端请求分析数据（携带 video_id）
        │
        ▼
后端查询 Redis 缓存
        │
    ┌───┴───┐
    │ 命中？ │
    └───┬───┘
   是   │   否
    │   │   └── 查询 analysis_result 表
    │   │         │
    │   │     ┌───┴───┐
    │   │     │ 存在？ │
    │   │     └───┬───┘
    │   │    是   │   否
    │   │     │   │   └── 查询 comment 表实时计算
    │   │     │   │         │
    │   │     │   │    ┌────┴────┐
    │   │     │   │    │ 多维度统计 │
    │   │     │   │    └────┬────┘
    │   │     │   │         │
    │   │     │   │    存入 analysis_result 表
    │   │     │   │         │
    │   │     │   └────┬────┘
    │   │     │        │
    │   └─────┴────────┘
    │            │
    │      写入 Redis（TTL=30min）
    │            │
    └──────┬─────┘
           │
    返回 JSON 给前端
           │
    前端渲染图表
```

---

# 9. 权限设计矩阵

| 功能/页面 | 游客 | 普通用户 | 管理员 |
|-----------|------|----------|--------|
| 登录/注册 | ✓ | ✓ | ✓ |
| 首页概览卡片 | ✓ | ✓ | ✓ |
| 完整可视化大屏 | ✗ | ✓ | ✓ |
| 评论采集 | ✗ | ✓ | ✓ |
| 数据清洗触发 | ✗ | ✓ | ✓ |
| 评论列表查看 | ✗ | ✓ | ✓ |
| 视频切换 | ✗ | ✓ | ✓ |
| 个人中心 | ✗ | ✓ | ✓ |
| 用户管理 | ✗ | ✗ | ✓ |

**后端实现**：SpringBoot拦截器 + 自定义注解 `@RequireRole`
```java
@RequireRole(role = 1)  // 仅管理员
@GetMapping("/admin/users")
public Result listUsers() { ... }
```

---

# 10. API 接口清单

## 10.1 用户接口 `/api/user`

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | `/api/user/register` | 用户注册 | 无 |
| POST | `/api/user/login` | 用户登录 | 无 |
| GET | `/api/user/info` | 获取当前用户信息 | 登录 |
| PUT | `/api/user/password` | 修改密码 | 登录 |
| POST | `/api/user/logout` | 退出登录 | 登录 |

### 注册请求体
```json
{
  "phone": "13800138000",
  "password": "abc123",
  "confirmPassword": "abc123"
}
```

### 登录响应体
```json
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "userId": 1,
    "phone": "138****8000",
    "role": 0
  }
}
```

## 10.2 采集接口 `/api/collect`

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | `/api/collect/start` | 启动采集任务 | 登录 |
| GET | `/api/collect/status?videoId=xxx` | 查询采集状态 | 登录 |

### 启动采集请求体
```json
{
  "videoId": "7613237683142223214"
}
```

### 状态响应
```json
{
  "code": 200,
  "data": {
    "videoId": "7613237683142223214",
    "status": "RUNNING",
    "progress": "已采集 156 条评论..."
  }
}
```

## 10.3 数据清洗接口 `/api/clean`

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | `/api/clean/run` | 执行清洗入库 | 登录 |
| GET | `/api/clean/files` | 查询待清洗文件列表 | 登录 |

## 10.4 评论接口 `/api/comment`

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/api/comment/list` | 分页查询评论 | 登录 |
| GET | `/api/comment/search` | 关键词搜索评论 | 登录 |

### 查询参数
```
?videoId=7613237683142223214&page=1&size=20&region=山东&sentiment=1&sort=likes_desc
```

## 10.5 可视化接口 `/api/analysis`

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/api/analysis/summary` | 概览统计 | 游客可访 |
| GET | `/api/analysis/region` | 地区分布 | 登录 |
| GET | `/api/analysis/sentiment` | 情感分布 | 登录 |
| GET | `/api/analysis/trend` | 时间趋势 | 登录 |
| GET | `/api/analysis/top-comments` | 高赞排行 | 登录 |
| GET | `/api/analysis/videos` | 已分析视频列表 | 登录 |

### 通用参数
```
?videoId=7613237683142223214
```

## 10.6 管理员接口 `/api/admin`

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/api/admin/users` | 用户列表 | 管理员 |
| DELETE | `/api/admin/user/{id}` | 删除用户 | 管理员 |

---

# 11. 数据库建表语句

```sql
CREATE DATABASE IF NOT EXISTS dy_comment DEFAULT CHARSET utf8mb4;
use dy_comment;

-- 用户表
CREATE TABLE `user` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `username` VARCHAR(50) NOT NULL COMMENT '用户名',
  `phone` VARCHAR(11) NOT NULL COMMENT '手机号',
  `password` VARCHAR(128) NOT NULL COMMENT 'BCrypt加密密码',
  `role` TINYINT NOT NULL DEFAULT 0 COMMENT '角色: 0=普通用户, 1=管理员',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '注册时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_phone` (`phone`),
  INDEX `idx_role` (`role`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 评论表
CREATE TABLE `comment` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '评论ID',
  `video_id` VARCHAR(32) NOT NULL COMMENT '视频编号',
  `nickname` VARCHAR(100) NOT NULL COMMENT '评论用户昵称',
  `region` VARCHAR(50) NOT NULL COMMENT '用户归属地区',
  `publish_time` DATETIME NOT NULL COMMENT '评论发布时间',
  `content` TEXT NOT NULL COMMENT '评论内容',
  `likes` INT NOT NULL DEFAULT 0 COMMENT '点赞数',
  `sentiment` TINYINT NOT NULL DEFAULT 0 COMMENT '情感: 1=积极, 0=中性, -1=消极',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '入库时间',
  PRIMARY KEY (`id`),
  INDEX `idx_video_id` (`video_id`),
  INDEX `idx_video_region` (`video_id`, `region`),
  INDEX `idx_video_time` (`video_id`, `publish_time`),
  INDEX `idx_video_likes` (`video_id`, `likes`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评论数据表';

-- 分析结果表
CREATE TABLE `analysis_result` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '记录ID',
  `video_id` VARCHAR(32) NOT NULL COMMENT '视频编号',
  `total_comments` INT NOT NULL DEFAULT 0 COMMENT '评论总数',
  `total_users` INT NOT NULL DEFAULT 0 COMMENT '参与用户数',
  `total_likes` BIGINT NOT NULL DEFAULT 0 COMMENT '总点赞数',
  `avg_likes` DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '平均点赞数',
  `region_stats` TEXT COMMENT '地区统计JSON',
  `sentiment_stats` TEXT COMMENT '情感统计JSON',
  `daily_stats` TEXT COMMENT '每日统计JSON',
  `top_comments` TEXT COMMENT '高赞评论JSON',
  `analyzed_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '分析时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_video_id` (`video_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='分析结果表';

-- 数据隔离: 为 comment 表添加 user_id 字段
ALTER TABLE comment ADD COLUMN user_id BIGINT NULL COMMENT '用户ID' AFTER id;
-- 为 analysis_result 表添加 user_id 字段（可选，用于后续扩展）
ALTER TABLE analysis_result ADD COLUMN user_id BIGINT NULL COMMENT '用户ID' AFTER id;
```
-- 管理员
INSERT INTO dy_comment.user (username, phone, password, role, created_at)
VALUES ('管理员', '10000000000', MD5('123456'), 1, NOW());
INSERT INTO dy_comment.user (username, phone, PASSWORD, role, created_at)
VALUES ('管理员', '13800000000', MD5('123456'), 1, NOW());

---

# 12. 技术架构

## 12.1 总体架构图

```
┌─────────────────────────────────────────────────────────┐
│                    前端 (Vue3 + Vite)                    │
│  ┌──────┐ ┌──────────┐ ┌──────┐ ┌──────┐ ┌──────────┐ │
│  │Login │ │Dashboard │ │Collect│ │Comments│ │Admin     │ │
│  └──────┘ └──────────┘ └──────┘ └──────┘ └──────────┘ │
│  Element Plus │ ECharts │ Pinia │ Vue Router │ Axios   │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTP/HTTPS (JSON)
                       ▼
┌─────────────────────────────────────────────────────────┐
│                   Nginx (反向代理)                       │
│              静态资源 │ /api/* 代理至后端                  │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────┐
│             后端 (SpringBoot 2.7.x)                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │Controller│ │Service   │ │Mapper    │ │Interceptor│  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│  JWT Auth │ BCrypt │ MyBatis-Plus │ FastJSON           │
└──────┬──────────────────────┬───────────────────────────┘
       │                      │
       ▼                      ▼
┌──────────┐          ┌──────────────┐
│  MySQL   │          │    Redis     │
│  8.0     │          │   缓存/JWT    │
└──────────┘          └──────────────┘
       ▲
       │ Python脚本调用
       │
┌──────────────────────┐
│  Python 采集模块      │
│  Selenium / Requests │
│  Py_Data/Datas/      │
└──────────────────────┘
```

## 12.2 技术栈明细

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 前端框架 | Vue3 | 3.3+ | 页面组件化开发 |
| 构建工具 | Vite | 5.x | 前端构建打包 |
| UI组件库 | Element Plus | 2.x | 表单、表格、布局 |
| 可视化 | ECharts | 5.x | 图表渲染（地图/饼图/折线图） |
| 状态管理 | Pinia | 2.x | 全局状态（用户信息/当前视频） |
| 路由 | Vue Router | 4.x | 前端路由与导航守卫 |
| HTTP客户端 | Axios | 1.x | 请求封装与拦截 |
| CSS预处理 | SCSS | - | 样式编写 |
| 后端框架 | SpringBoot | 2.7.x | 业务逻辑与接口 |
| ORM | MyBatis-Plus | 3.5.x | 数据库操作 |
| 数据库 | MySQL | 8.0 | 业务数据存储 |
| 缓存 | Redis | 7.x | JWT存储、分析结果缓存 |
| 安全 | BCrypt + JWT | - | 密码加密 + 身份认证 |
| JSON处理 | FastJSON2 | 2.x | JSON序列化 |
| 连接池 | HikariCP | - | 数据库连接池 |
| 爬虫 | Python 3 + Selenium | 3.10+ | 评论数据采集 |
| 数据分析 | Python Pandas + Jieba | - | 数据处理与中文分词 |
| 反向代理 | Nginx | 1.24+ | 静态资源 + 接口代理 |

---

# 13. 项目目录结构

> 项目根路径：`D:\idea_workspace\DY-Comment`

```
DY-Comment/                                    # 项目根目录
│
├── Py_Data/                                   # 【已存在】Python采集与数据处理模块
│   ├── douyin_comment.py                      # 抖音评论采集主脚本（DrissionPage）
│   ├── config_browser.py                      # Chrome浏览器路径配置脚本
│   ├── Datas/                                 # 原始CSV数据存放目录
│   │   └── {video_id}.csv                     # 以视频编号命名的原始数据文件
│   ├── clean/                                 # 【待创建】清洗与情感分析脚本
│   │   ├── clean_main.py                      # 数据清洗入库主脚本
│   │   ├── analysis_engine.py                 # 情感分析引擎（情感词典+规则匹配）
│   │   └── sentiment_dict/                    # 中文情感词典
│   │       ├── positive_words.txt             # 正面情感词库
│   │       └── negative_words.txt             # 负面情感词库
│   └── requirements.txt                       # Python依赖清单
│
├── dy-backend/                                # 【待创建】SpringBoot后端项目
│   ├── src/main/java/com/dy/comment/
│   │   ├── DyCommentApplication.java          # SpringBoot启动类
│   │   ├── config/                            # 配置类（CORS、Redis、MVC拦截器）
│   │   ├── controller/                        # REST控制器层
│   │   │   ├── UserController.java            # 用户注册/登录/信息接口
│   │   │   ├── CollectController.java         # 采集任务启动/状态接口
│   │   │   ├── CommentController.java         # 评论分页查询/搜索接口
│   │   │   ├── AnalysisController.java        # 可视化分析数据接口
│   │   │   └── AdminController.java           # 管理员用户管理接口
│   │   ├── service/                           # 业务逻辑层接口
│   │   │   ├── UserService.java
│   │   │   ├── CollectService.java
│   │   │   ├── CommentService.java
│   │   │   ├── AnalysisService.java
│   │   │   └── impl/                          # 接口实现类
│   │   ├── mapper/                            # MyBatis-Plus映射器
│   │   │   ├── UserMapper.java
│   │   │   ├── CommentMapper.java
│   │   │   └── AnalysisResultMapper.java
│   │   ├── entity/                            # 数据库实体类
│   │   │   ├── User.java
│   │   │   ├── Comment.java
│   │   │   └── AnalysisResult.java
│   │   ├── dto/                               # 数据传输对象（请求/响应）
│   │   ├── interceptor/                       # JWT登录拦截器
│   │   ├── annotation/                        # 自定义注解（@RequireRole角色鉴权）
│   │   └── utils/                             # 工具类（JwtUtil、BcryptUtil）
│   ├── src/main/resources/
│   │   ├── application.yml                    # 主配置文件
│   │   ├── application-dev.yml                # 开发环境配置
│   │   └── mapper/                            # MyBatis XML映射文件（如有）
│   └── pom.xml                                # Maven依赖管理
│
├── dy-frontend/                               # 【待创建】Vue3前端项目
│   ├── src/
│   │   ├── views/                             # 页面级组件
│   │   │   ├── LoginView.vue                  # 登录页
│   │   │   ├── RegisterView.vue               # 注册页
│   │   │   ├── DashboardView.vue              # 数据大屏首页
│   │   │   ├── CollectView.vue                # 数据采集页
│   │   │   ├── CommentListView.vue            # 评论数据列表页
│   │   │   ├── ProfileView.vue                # 个人中心页
│   │   │   └── admin/
│   │   │       └── UserManageView.vue         # 用户管理页（管理员）
│   │   ├── components/                        # 可复用可视化组件
│   │   │   ├── AppLayout.vue                  # 全局布局（侧栏+顶栏+内容区）
│   │   │   ├── RegionMap.vue                  # 全国地区分布地图
│   │   │   ├── SentimentPie.vue               # 情感占比环形饼图
│   │   │   ├── TrendLine.vue                  # 评论时间趋势折线图
│   │   │   ├── TopComments.vue                # 高赞评论排行榜
│   │   │   └── StatsCards.vue                 # 数据概览统计卡片
│   │   ├── router/                            # Vue Router路由配置
│   │   ├── stores/                            # Pinia全局状态管理
│   │   ├── api/                               # Axios请求封装层
│   │   └── utils/                             # 前端工具函数
│   ├── package.json                           # NPM依赖管理
│   └── vite.config.js                         # Vite构建配置
│
├── .venv/                                     # 【已存在】Python 3.12虚拟环境
│   └── Lib/site-packages/                     # DrissionPage、requests 等
│
├── .claude/                                   # Claude Code配置目录
│   └── settings.json
│
└── 需求设计（抖音多视频评论分析与可视化平台）.md.md  # 本文档
```

---

# 14. 缓存策略

| 缓存对象 | Key格式 | TTL | 更新策略 |
|----------|---------|-----|----------|
| JWT Token | `token:{token}` | 24h | 登录写入，退出删除 |
| 分析概览 | `analysis:summary:{videoId}` | 30min | 首次查询写入，清洗后删除 |
| 地区统计 | `analysis:region:{videoId}` | 30min | 同上 |
| 情感统计 | `analysis:sentiment:{videoId}` | 30min | 同上 |
| 时间趋势 | `analysis:trend:{videoId}` | 30min | 同上 |
| 高赞排行 | `analysis:top:{videoId}` | 30min | 同上 |
| 视频列表 | `analysis:videos` | 10min | 新分析完成后删除 |

**缓存更新触发点**：数据清洗入库完成后，批量删除该 `videoId` 相关的所有缓存Key。

---

# 15. 安全设计

| 安全点 | 措施 |
|--------|------|
| 密码存储 | BCrypt 加盐哈希，不可逆 |
| 传输安全 | JWT Token + 请求头 Authorization |
| 防SQL注入 | MyBatis-Plus 参数化查询（`#{}`占位符） |
| 跨域控制 | CORS配置限定前端域名 |
| XSS防护 | 前端输出转义 + 后端内容过滤 |
| 接口鉴权 | 拦截器统一校验JWT + 角色注解 |
| 文件安全 | 清洗后自动删除本地CSV，防止数据泄露 |
| 手机号脱敏 | 对外展示统一中间四位星号 `138****8000` |

---

# 16. 部署架构

```
┌─────────────────────────────────────┐
│            用户浏览器                 │
└──────────────┬──────────────────────┘
               │ HTTP
               ▼
┌─────────────────────────────────────┐
│           Nginx :80                  │
│  /           → dy-frontend/dist/    │
│  /api/*      → localhost:8080/*     │
└──────────────┬──────────────────────┘
               │
       ┌───────┴────────┐
       ▼                 ▼
┌────────────┐   ┌──────────────┐
│ SpringBoot │   │  静态资源     │
│ :8080      │   │  (Nginx)     │
└─────┬──────┘   └──────────────┘
      │
      ├──────────┐
      ▼          ▼
┌────────┐ ┌──────────┐
│ MySQL  │ │  Redis   │
│ :3306  │ │  :6379   │
└────────┘ └──────────┘
```

**建议部署环境**：
- 操作系统：Linux（CentOS 7+ / Ubuntu 20.04+）
- JDK 8+
- Node.js 18+
- Python 3.10+
- MySQL 8.0
- Redis 7.x
- Nginx 1.24+

---

# 17. 后续可扩展功能

| 优先级 | 功能 | 说明 |
|--------|------|------|
| P1 | 词云图 | 评论关键词分词后生成词云 |
| P1 | 数据导出 | Excel/CSV格式导出分析报告 |
| P2 | 批量采集 | 一次输入多个视频编号，排队采集 |
| P2 | 定时采集 | Cron定时任务，自动更新评论数据 |
| P2 | 多视频对比 | 选择两个视频，并排对比分析结果 |
| P3 | 评论回复链 | 采集评论下的回复数据 |
| P3 | 敏感词过滤 | 自动标记/隐藏含敏感词的评论 |
| P3 | 深色主题 | 前端深色模式切换 |

---

# 18. 附录：数据字典

## 18.1 地区编码映射（涉及时的中国行政区划）

系统直接使用抖音接口返回的省份/直辖市名称，不做额外编码映射。支持的地区包括：

华北：北京、天津、河北、山西、内蒙古
东北：辽宁、吉林、黑龙江
华东：上海、江苏、浙江、安徽、福建、江西、山东
华中：河南、湖北、湖南
华南：广东、广西、海南
西南：重庆、四川、贵州、云南
西北：陕西、甘肃、青海、宁夏、新疆
港澳台：香港、澳门、中国台湾
海外：美国、日本、韩国、英国 等（统一归类为"海外"）

## 18.2 情感分类标准

| 分类 | 编码值 | 判定标准 |
|------|--------|----------|
| 积极 | 1 | 正面情感词得分 > 负面情感词得分 |
| 中性 | 0 | 无情感词 或 正负面得分相等 |
| 消极 | -1 | 负面情感词得分 > 正面情感词得分 |

## 18.3 采集任务状态

| 状态 | 说明 |
|------|------|
| IDLE | 空闲，无采集任务 |
| RUNNING | 采集中 |
| SUCCESS | 采集完成 |
| FAILED | 采集失败（含错误信息） |

---

*文档版本：v2.0*
*最后更新：2026-05-12*
