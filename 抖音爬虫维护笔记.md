# 抖音评论爬虫 — 维护笔记

## 一句话总结

> **抖音页面改版了**，评论区从"滚动网页本身"变成了"滚动网页内部的一个小盒子"，所以以前的滚动代码失效了。

---

## 一、之前为什么能正常工作？

以前的抖音页面结构大致是这样：

```
┌─────────────────────────────┐
│         视频播放区             │
│                             │
├─────────────────────────────┤
│         评论区标题            │
│  ┌───────────────────────┐  │
│  │ 评论1                  │  │
│  │ 评论2                  │  │
│  │ 评论3                  │  │
│  │ ...                   │  │
│  └───────────────────────┘  │
│         底部信息             │
├─────────────────────────────┤
│       (.Rcc71LyU) ← 触底标记  │
└─────────────────────────────┘
         ↑ 整个页面 = 一个长网页
         ↑ 滚动鼠标 = 滚动整个页面
```

- **滚动的是网页本身**（`document.body`），`scrollHeight` 远大于 `clientHeight`
- 滚动到底部时，`.Rcc71LyU` 这个元素进入视野，触发加载更多评论
- 所以 `dp.scroll.to_see(".Rcc71LyU")` 就能工作

---

## 二、现在为什么不能了？

抖音前端重构后，页面变成了这样：

```
┌─────────────────────────────┐
│         视频播放区             │← 固定在顶部，不动
│                             │
│┌───────────────────────────┐│
││  评论区标题（固定高度）      ││
││  ┌─────────────────────┐  ││
││  │ 评论1                │  ││
││  │ 评论2                │  ││  ← 这个"小盒子"内部
││  │ 评论3                │  ││    自己滚动
││  │ ...                 │  ││
││  │                     │  ││
││  └─────────────────────┘  ││
│└───────────────────────────┘│
│   ┌──────────────────┐      │
│   │ 推荐视频列表...   │      │
│   └──────────────────┘      │
└─────────────────────────────┘
   ↑ 整个页面高度 = 刚好一个屏幕
   ↑ 浏览器 body 无法滚动
```

关键变化：

| 项目 | 旧版 | 新版 |
|------|------|------|
| 谁负责滚动？ | 浏览器网页（body） | 内部 DIV（`.parent-route-container`） |
| body scrollHeight | 几千像素 | = 屏幕高度（无溢出） |
| CSS 类名 `.Rcc71LyU` | 存在 | 已删除 |
| `#commentInput` | 存在 | 已删除 |
| 评论区触发方式 | 滚动到页面底部 | 滚动内部容器 |

所以旧代码 `dp.scroll.to_see(".Rcc71LyU")` 相当于对着一堵墙喊"开门"——那个元素根本不存在了。

---

## 三、修复方法（一句话版）

**不滚动网页了，改成用 JS 滚动内部的盒子：**

```python
# 旧方案（失效）：
dp.scroll.to_see(".Rcc71LyU")

# 新方案：
dp.run_js('document.querySelector(".parent-route-container").scrollBy(0, 800)')
```

`.parent-route-container` 就是抖音评论区所在的那个内部滚动容器。

---

## 四、如何自己诊断这类问题？

如果以后又出现"页面不滚动"、"评论加载不出来"，按以下步骤排查：

### 第一步：打开浏览器开发者工具

在抖音视频页面按 `F12` → 切到 **Console（控制台）**。

### 第二步：执行这三条命令

```javascript
// 1. 看看 body 能不能滚动
document.body.scrollHeight > document.body.clientHeight + 100
// → false 说明 body 不能滚动了（新版特征）

// 2. 找出所有能滚动的容器
// 直接执行，会列出所有可滚动的内部容器
document.querySelector(".parent-route-container")
// → null 说明容器名也变了，需要重新找

// 3. 如果容器名变了，用这条命令自动扫描
// （在 Console 里执行）
var els = document.querySelectorAll('*');
var found = [];
for (var i = 0; i < els.length; i++) {
  var e = els[i];
  if (e.scrollHeight > e.clientHeight + 50 &&
      e.clientHeight > 300 &&
      getComputedStyle(e).overflowY === 'auto') {
    found.push(e.tagName + '#' + e.id + '.' + e.className.slice(0,60));
  }
}
console.log(found);
// → 输出结果就是新的滚动容器
```

### 第三步：把结果告诉我

把 Console 输出的信息复制给我，我就能告诉你新的选择器是什么，帮你改好。

---

## 五、预防措施（推荐加到脚本里）

每次运行爬虫前自动检测一次，结构变了立刻报错，不闷头跑：

```python
def preflight_check(dp):
    """采集前的快速体检"""
    # 检查滚动容器是否存在
    ok = dp.run_js('return !!document.querySelector(".parent-route-container")')
    if not ok:
        print("❌ 滚动容器不存在，页面可能已改版！")
        # 让脚本自动扫描新容器
        containers = dp.run_js("""return JSON.stringify(
            Array.from(document.querySelectorAll('*')).filter(e =>
                e.scrollHeight > e.clientHeight + 50 &&
                e.clientHeight > 300 &&
                getComputedStyle(e).overflowY === 'auto'
            ).map(e => ({tag: e.tagName, id: e.id, cls: e.className.slice(0,60)}))
        )""")
        print(f"   当前可用滚动容器: {containers}")
        return False
    print("✅ 滚动容器正常")
    return True
```

---

## 六、核心教训

| # | 教训 |
|---|------|
| 1 | **抖音是 SPA（单页应用），前端频繁重构**。以前能跑不代表一直能跑 |
| 2 | **不要死磕某个 CSS 类名**，抖音用 hash 类名（如 `.Rcc71LyU`），每次改版都可能变 |
| 3 | **诊断比瞎猜重要**。打开控制台看 3 条信息，比改 10 次代码有效 |
| 4 | **脚本启动时做一次"体检"**，结构不对立刻报错，节省排查时间 |

---

## 七、常见问题速查

| 现象 | 最可能原因 | 怎么查 |
|------|-----------|--------|
| 页面打开但一直白屏 | 网络拦截/Chrome 版本 | F12 → Network 看请求 |
| 只采集到 5~10 条就停了 | 滚动没触发 | 执行第二步的 JS 找滚动容器 |
| `has_more = 1` 但没新数据 | 滚动容器不对/没滚动到底 | 检查容器 `scrollHeight` |
| 报 Unicode 错误 |  Windows 控制台编码 | 加 `PYTHONIOENCODING=utf-8` |
| URL 跳转到 `note/` 页面 | 视频不存在或不是视频 | 确认视频 ID 是否正确 |

---

> **最后一条铁律：** 抖音不改版是例外，改版是常态。别问"为什么以前可以现在不行"，问就是"抖音又改前端了"。
